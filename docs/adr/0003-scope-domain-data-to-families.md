# Scope domain data to families

Family is the ownership boundary for recipes, recipe tags, ingredients, stores, store sections, calendar entries, and saved shopping lists. Users may join multiple families and work within one family's data at a time. This supports a shared household cookbook without making records global to every authenticated user, at the cost of requiring family context and authorization throughout the application.
